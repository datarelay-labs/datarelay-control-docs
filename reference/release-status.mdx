---
title: "Release Status"
description: "Current DataRelay Control release identity and immutable Git evidence."
---

# Release Status

The product source currently identifies the release line as **GA v1.0.2** in both the repository README and `CHANGELOG.md`.

Git evidence must be distinguished from that product identity:

| Track | Current evidence |
|---|---|
| `v1.0.0` | Immutable Git tag present |
| `1.0.1` | Recorded in `CHANGELOG.md`; no `v1.0.1` tag observed in the current remote tag audit |
| `1.0.2` | README labels GA v1.0.2 and `CHANGELOG.md` records it; no `v1.0.2` tag observed in the current remote tag audit |
| `main-v2` | Current development integration branch; contains changes beyond the last tag-proven baseline |

GitHub Releases currently exposes no release objects for this repository.

## Documentation rule

This site follows the current `main-v2` implementation for product behavior, while release claims remain explicit about whether they are source-documented or immutable-tag proven.

For production use, pin the exact tag or commit you intend to run. Do not treat a mutable branch name as immutable release evidence.
